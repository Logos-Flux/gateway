# Gateway Configuration
# Copy to .env and set values

# API authentication token (optional — if unset, auth is disabled)
GATEWAY_API_TOKEN=

# ntfy.sh notifications
NTFY_ENABLED=false
NTFY_TOPIC=gateway
NTFY_URL=https://ntfy.sh

# Scheduler tuning
PREEMPT_PRIORITY_GAP=2
IDLE_TIMEOUT_SECONDS=300
